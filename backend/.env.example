# =============================================================
# Variables de Entorno - Second Brain AI
# =============================================================
# Copia este archivo como .env y completa los valores:
#   cp .env.example .env  (Linux/Mac)
#   copy .env.example .env  (Windows)
# =============================================================

# --- Aplicación ---
APP_NAME="Second Brain AI"
APP_VERSION="0.1.0"
DEBUG=true

# --- Base de Datos ---
# Formato: postgresql+asyncpg://usuario:contraseña@host:puerto/nombre_db
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/secondbrain_db

# --- Seguridad / JWT ---
# Genera una clave secreta con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu_clave_secreta_aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- IA / OpenAI (para pasos avanzados) ---
# OPENAI_API_KEY=sk-tu-api-key-aqui
# OPENAI_MODEL=gpt-4o-mini

# --- CORS ---
# Orígenes permitidos separados por coma
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
